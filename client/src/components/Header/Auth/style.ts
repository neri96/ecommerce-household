import styled from 'styled-components';
import * as v from '../../../constants/variables';

import { NavIcon } from '../style';

import { ModalLoading, ModalHeader, Input, Button } from '../../includes/Modal/style';

export {
    NavIcon,
    ModalLoading,
    ModalHeader,
    Input,
    Button
};